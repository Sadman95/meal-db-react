// display to ui cart:
/* const addItems = () =>{
    const isExist = localStorage.getItem('cart');
    if(isExist){
        const itemsObj = JSON.parse(localStorage.getItem('cart'));
        const getValues = Object.values(itemsObj);
        let total = 0;
        for (const value of getValues) {
            total = total + value;
            return total;
        }
        console.log(total)
}
}
export {addItems} */